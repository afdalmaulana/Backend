const userController = require("./userController");
const blogController = require("./blogController")
const profileController = require("./profileController")

module.exports = {
    userController, blogController, profileController
}